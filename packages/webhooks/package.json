{
  "name": "@IssueForge/webhooks",
  "version": "0.1.0",
  "private": true,
  "description": "Keeps Github and Jira issues in sync.",
  "author": "Matthew Howlett <mhowlett@applied-ml.dev>",
  "license": "ISC",
  "homepage": "https://github.com/howl0893/IssueForge",
  "keywords": [
    "IssueForge"
  ],
  "dependencies": {
    "@IssueForge/clients": "^0.1.0",
    "@IssueForge/db": "^1.0.0",
    "@IssueForge/handlers": "^0.1.0",
    "@IssueForge/utils": "^0.1.0"
  },
  "scripts": {
    "build": "rimraf build && tsc",
    "start:watch": "rimraf build && tsc --watch",
    "start": "yarn build && concurrently \"yarn start:watch\" \"nodemon -r dotenv/config build/index.js\"",
    "start:prod": "node build/index.js"
  },
  "engines": {
    "node": ">= 10.13.0"
  }
}
